# Acoustic Dataset and Spectrogram Engineering
Notebook artifacts not found. Run the acoustic pipeline so `acoustic_metrics_table.csv` and related files exist, then re-execute `scripts/generate_report_markdown.py`.
